[project]
site_name = "OBS Flow Blog"
docs_dir = "blog"

[project.theme]
variant = "classic"
features = [
    "navigation.indexes"
]

[project.plugins.search]

[project.plugins.blog]
blog_dir = "."
post_dir = "posts"
