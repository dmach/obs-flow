[project]
name = "obs-flow"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/common",
    "packages/client",
    "packages/cli",
    "packages/server",
]
